# https://doc.traefik.io/traefik/routing/services/#configuration-examples
http:
  routers:
    webserver:
      entryPoints:
        - "web"
      rule: "PathPrefix(`/`)"
      service: webserver

  services:
    webserver:
      loadBalancer:
        servers:
          - url: "http://webserver:3001"
